export enum FoodAction {
  ADD_ITEM = "ADD_ITEM",
  REMOVE_ITEM = "REMOVE_ITEM",
  CLEAR_CART = "CLEAR_CART",
}

export enum StatusProgress {
  CART = "cart",
  CHECKOUT = "checkout",
  DEFAULT = "",
}
